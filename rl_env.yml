name: rl-exp            # default is root
channels:
    - conda
dependencies:         # everything under this, installed by conda
    - numpy
    - scipy
    - pandas
    - Tensorflow
    - matplotlib
    - keras
    - pip:                # everything under this, installed by pip
        - gym
